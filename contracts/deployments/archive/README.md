# Archived Sepolia deployment JSON

Superseded one-off dumps from early TokenFactory / DEX experiments. **Do not** treat these addresses as live. The app reads `frontend/src/config/contracts.js`. Current DEX deploys write `../dex-<chainId>.json`.
