node_modules

# Hardhat compile outputs (regenerate with `npx hardhat compile` in contracts/)
contracts/artifacts/
contracts/cache/

# Local link to sibling boing.network repo (file:../boing.network/boing-sdk in frontend/package.json)
/boing.network
backend/.wrangler/tmp/

# Frontend build outputs (Vite → dist; legacy CRA → build)
frontend/dist/
frontend/build/
# Wrangler Pages Functions local bundle output
frontend/_worker.bundle

# Playwright
frontend/test-results/
frontend/playwright-report/

# Native DEX indexer CLI state (reserve history)
frontend/.native-dex-indexer-state.json

# Local secrets (never commit)
.env
**/.env
.env.local
**/.env.local
frontend/.env.local
!.env.example
!**/.env.example
!.env.production
!**/.env.production

# Windows accidental reserved-name file
nul
